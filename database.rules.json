{
  "rules": {
    "//": [
      "Fully closed to clients. The puzzle table lives here, and its rows",
      "contain the answers -- the previous `.read: auth !== null` rule handed",
      "every scheduled solution to anyone signed in, including anonymous",
      "visitors. Puzzles are now served (without answers) by /api/puzzle/today,",
      "and the Admin SDK bypasses these rules."
    ],
    ".read": false,
    ".write": false
  }
}
